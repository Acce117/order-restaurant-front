# Order-restaurant-front

## Prerequisites

- Node.js: use LTS (≥ 22).

## Steps to run project

1. Clone the project

2. Install dependencies

    npm:

    ```bash
    npm install
    ```

3. In `/src/environments/environment.development.ts` set `API_PATH` property with the backend domain;

4. Run development server `npm run start:dev`
